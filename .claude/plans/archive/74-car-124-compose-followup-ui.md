# CAR-124: Compose follow-up plan UI fixes

1. Replace broken custom toggle with shared `Toggle` (knob visible when on)
2. Cap medallion body with `max-h` + `overflow-y-auto` so expanded editors scroll inside
3. Footer copy: premium/`followups:auto` → "Auto-cancels if they reply."; else confirm-with-first-name copy

{
  "name": "careervine-mcp",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "MCP server exposing CareerVine (contacts, email, outreach, upkeep, calendar) to Claude Code",
  "scripts": {
    "start": "tsx --tsconfig tsconfig.json server.ts",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^20",
    "tsx": "^4.20.0",
    "typescript": "^5"
  }
}
